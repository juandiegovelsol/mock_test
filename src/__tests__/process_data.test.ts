const { processData, printData } = require('../event_loop/process_data');

describe('processData function', () => {
  it('should double each number in the input array', () => {});
});

describe('printData function', () => {
  it('should print each element of the input array', () => {});
});
